export enum Frequency {
  daily = "daily",
  weekly = "weekly",
  monthly = "monthly",
  yearly = "yearly",
}

export enum Status {
  active = "active",
  paused = "paused",
  cancelled = "cancelled",
}
